spring.datasource.url=jdbc:postgresql://localhost/mimimetr
spring.datasource.username=postgres
spring.datasource.password=A.Chemakin00
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
#spring.flyway.baseline-on-migrate = true

upload.path=/home/lexa/IdeaProjects/mimimetr/src/main/resources/photos

# create sequence hibernate_sequence start 1 increment 1
# create table kitty (id int4 generated by default as identity, name varchar(255), photo varchar(255), rating float8 not null, primary key (id))
# create table user_role (user_id int8 not null, roles varchar(255))
# create table usr (id int8 not null, password varchar(255), username varchar(255), primary key (id))
# alter table if exists user_role add constraint FKfpm8swft53ulq2hl11yplpr5 foreign key (user_id) references usr
